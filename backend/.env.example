PORT=3000

# Local dev — SQLite (no PostgreSQL needed)
USE_SQLITE=true
SQLITE_PATH=./database/restaurant_bot.db

# Production — set USE_SQLITE=false and provide DATABASE_URL
# USE_SQLITE=false
# DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/restaurant_bot
